import { IStylesheetDTO } from "./Stylesheet.interface";
import Stylesheet from "./index";

const defaultStylesheet: IStylesheetDTO = {
  id: "fakeStylesheet",
};

describe("Stylesheet entity", () => {
  it("", () => {});
});
